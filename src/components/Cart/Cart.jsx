export function Cart() {
  return (
    <div data-testid="Cart">
      <p>Cart</p>
    </div>
  );
}

// Maps over cartItems
// CartItem component
// Allow amount editing on cart items?
// Checkout button
